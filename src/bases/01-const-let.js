// Variables y Constantes
// const son variables que no se van a renombrar, las utilizamos para crear esos
// espacios en memoria para almacenar informacion. Si no voy a cambiar los valores
// ponerlo como const
const nombre = "Fernando";
const apellido = "Paz";

let valorDado = 5;
valorDado = 4;

console.log(nombre, apellido, valorDado);

if(true) {
    let valorDado = 6;
    console.log(valorDado);

    const nombre = 'Peter';
    console.log(nombre);
}

console.log(valorDado);